import { CustomFieldType } from "@/interfaces/form";
import { Job } from "@/interfaces/job";

type Props = {
  job: Job;
  form: CustomFieldType[];
};

function CandidateForm() {
  return <div>CandateForm</div>;
}

export default CandidateForm;
